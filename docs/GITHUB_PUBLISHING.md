# Later GitHub Publishing

These commands are only notes for a later publishing step. They were not executed.

```bash
git remote rename origin upstream
git remote add origin <SPATERE_EIGENE_GITHUB_ADRESSE>
git push -u origin feature/lilygo-t-display-s3
```
